
import { DesignBrief, Difficulty, Category, Submission, User, UserRanking } from './types';

export const MOCK_USERS: User[] = [];

export const MOCK_CHALLENGES: DesignBrief[] = [];

export const TOTAL_CHALLENGES_COUNT = 0; 

export const MOCK_SUBMISSIONS: Submission[] = [];

export const MOCK_RANKINGS: Record<string, UserRanking[]> = {
  week: [],
  month: [],
  year: [],
};

